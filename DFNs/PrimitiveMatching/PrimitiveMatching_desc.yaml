name: PrimitiveMatching
doc: DFN that finds primitives in a 2D image
input_ports:
    - name: image
      type: asn1SccFrame
      doc: 2D image captured by a camera
output_ports:
    - name: image
      type: asn1SccFrame
      doc: same image, with the contour that was matched to a primitive
    - name: string
      type: asn1SccT_String
      doc: string with the name of the matched primitive
implementations:
    - HuInvariants
