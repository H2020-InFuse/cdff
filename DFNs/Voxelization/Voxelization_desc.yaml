name: Voxelization
doc: DFN that voxelizes a 2D depth map
input_ports:
    - name: depth
      type: asn1SccFrame
      doc: depth map
output_ports:
    - name: octree
      type: asn1SccOctree
      doc: octree created from the input depth map
implementations:
    - Octree
